# GPT 每日通报可视化看板 - 更新日志

## v2.8.0 - 2026年5月29日

### 🎉 新增功能

#### 1. 非操占比列
- **位置**：DataTable 得分列右侧，7 列改为 8 列
- **算法**：非操作人数 = 花名册(排除中心操作+特殊岗位) + 外包人数；非操占比 = 非操作人数 ÷ (花名册在职 + 外包) × 100%
- **过滤逻辑**：九级单位=xx转运中心，二级部门≠中心操作，岗位∉{安检员, 仓库管理员, 环保袋管理维修员}
- **数据源**：花名册 `roster_MMDD.xlsx` + 外包 `outsourcing.xlsx`（每周更新）
- **外包数据**：`public/database/outsourcing.xlsx`，14个转运中心对应外包人数

#### 2. 非操明细弹窗
- 点击非操占比较字弹出明细
- 显示：花名册在职、外包人数、总人数、非操作人数、非操占比
- **各部门人数明细**：列出每个二级部门的人数，按人数降序

#### 3. 排休计划多段日期选择
- **之前**：一个人只能选一段日期（如 5/20-5/22）
- **现在**：支持添加多段不连续日期（如 5/20~5/22, 5/28~5/30）
- **操作**：点"添加日期段"选起止 → 添加 → 可重复添加多段
- **显示**：逗号分隔，如 `5/20~5/22, 5/28~5/30`
- **兼容**：旧数据 start/end 自动转换为 ranges[] 数组

#### 4. 数据管道自动化
- `process_data.py` 推送前自动执行 `npm run build:data`（Excel → JSON）
- 花名册 JSON 过大（153MB）不入库，由 CI 自动生成
- `.gitignore` 改为 `roster_*.json` 统一排除所有花名册 JSON

---

### 🔧 功能优化

#### 1. 效能异常弹窗增强
- 新增"全网同岗均值"和"均值偏离"两列
- 表格从 4 列扩展为 6 列：岗位 | 实际 | 目标 | 偏离 | 全网均值 | 均值偏离
- 列宽、字体、行高整体优化，视觉更清晰

#### 2. 导出报告修复
- 日工时高触发率字段名修正：`whHighRate` → `workHoursHighRate`

---

### 🗑️ 删除

- 长期未出勤弹窗移除"考勤负责人"编辑功能

---

### 🐛 Bug 修复

1. **0527/0528 数据不显示**：JSON 文件未生成导致网页只显示到 26 号（已补齐 14 个 JSON 文件）
2. **Office 锁文件入库**：`~$*.xlsx` 被提交到 git（已移除并优化 .gitignore）
3. **非操占比算法**：分子漏加外包人数（已修正）

---

### 📋 技术细节

#### 修改文件
- `src/hooks/useEnrichedData.ts`：非操占比计算 + 部门明细
- `src/components/DataTable.tsx`：非操列 + 点击明细弹窗
- `src/components/EfficiencyDetailModal.tsx`：全网均值/均值偏离列 + 间距优化
- `src/components/Attendance15DetailModal.tsx`：排休多段日期选择（数据结构 start/end → ranges[]）
- `src/components/Attendance7DetailModal.tsx`：删除考勤负责人
- `src/lib/reportGenerator.ts`：日工时高字段名修正
- `scripts/build-json-data.mjs`：外包 xlsx 自动转 JSON
- `C:\Users\0347\data-pipeline\process_data.py`：推送前自动 build:data
- `.gitignore`：roster_0525.json → roster_*.json

#### 新增文件
- `public/database/outsourcing.xlsx`：外包人数数据源
- `public/database/outsourcing.json`：外包人数 JSON（build:data 自动生成）

---

## v2.7.0 - 2026年5月22日

### 🎉 新增功能

#### 1. 通报总览图片生成（精致报告风）
- **功能**：点击通报弹窗中的"复制图片"按钮，自动生成精致报告风格的总览表格图片
- **输出**：图片直接复制到剪贴板，可直接 Ctrl+V 粘贴到微信
- **降级方案**：如果剪贴板 API 失败（如非 HTTPS 环境），会自动改为下载图片

---

### 🔧 功能优化

#### 1. 图片表格结构优化
- **之前**：表格 10 列（中心、得分、管幅综、管幅组、超目标综、超目标组、效能异常、绩效异常、连续出勤、长期未出勤）
- **现在**：表格 8 列（中心、得分、管幅、超目标、效能异常、绩效异常、连续出勤、长期未出勤）
- **管幅/超目标**：每列内显示两行（综合 + 组长），更紧凑清晰
- **行高**：34px → 48px，容纳两行文字

#### 2. 图片标红规则调整
- **绩效异常**：> 1个 → 红色加粗
- **薪资异常**：> 3% → 红色加粗
- **连续出勤**：> 3% → 红色加粗
- **长期未出勤**：> 3% → 红色加粗

#### 3. 图片标题栏布局优化
- **标题栏高度**：64px → 80px
- **标题文字**：居中 → 左上对齐（"GPT 数据总览"）
- **副标题**：在标题下方（"数据日期：2026-05-20"）
- **全区均分标签**：右侧白色半透明背景 + 文字居中，位置计算更准确

#### 4. 图片视觉统一
- **之前**：标题栏全宽、表头/数据行只渲染中间区域，看起来是两个独立的块
- **现在**：标题栏 + 表头 + 数据行全部全宽连成一体，深蓝色背景贯穿顶部
- **得分背景色**：#d1fae5（浅绿）/ #fef3c7（浅黄）/ #fecaca（浅红），视觉更柔和

#### 5. 图片边距压缩
- **左右边距**：24px → 4px，图片更紧凑
- **Canvas 尺寸**：动态计算，适配 8 列内容

---

### 🐛 Bug 修复

1. **标题栏与表头断开**：标题栏全宽、表头只渲染中间区域，视觉断开（已修复，现在全部全宽连成一体）
2. **左右留白过大**：边距 24px 太宽（已修复，压缩到 4px）
3. **斑马纹颜色错误**：#fafafa（浅灰）应该是 #f0f7ff（浅蓝）（已修复）
4. **得分背景色太浅**：#f0fdf4/#fefce8/#fef2f2 太浅看不出来（已修复，改为 #d1fae5/#fef3c7/#fecaca）
5. **文字对齐方式错误**：textAlign='right' 应该是 center（已修复）
6. **颜色判断条件错误**：salaryCount > 0 应该是 parseFloat(salaryCoverage) > 3（已修复）

---

### 📋 技术细节

#### 修改文件
- `src/components/ReportModal.tsx`：图片生成逻辑重构（Canvas API）
- `src/lib/reportGenerator.ts`：添加 att7Rate 字段
- `src/components/MetricHelpPanel.tsx`：版本修订历史更新（V2.7.0）

#### 关键代码变更
1. **Canvas 尺寸计算**：
   ```typescript
   const colWidths = [100, 70, 110, 110, 80, 80, 80, 100];
   const rowHeight = 48;
   const titleHeight = 80;
   const padding = 4;
   ```

2. **标题栏布局**：
   ```typescript
   ctx.fillStyle = '#1e3a5f';
   ctx.fillRect(0, 0, canvasWidth, titleHeight);
   ctx.font = 'bold 20px ...';
   ctx.textBaseline = 'top';
   ctx.fillText('GPT 数据总览', padding, 18);
   ```

3. **表头全宽渲染**：
   ```typescript
   ctx.fillStyle = '#1e3a5f';
   ctx.fillRect(0, titleHeight, canvasWidth, headerHeight);
   ```

4. **管幅/超目标两行显示**：
   ```typescript
   drawTwoLineCell(
     `综合: ${row.compositeScope.toFixed(1)}`,
     `组长: ${row.leaderScope.toFixed(1)}`,
     xPos, colWidths[2], scopeColor1, scopeColor2
   );
   ```

---

### 📋 使用说明

#### 生成通报图片（新）
1. 打开通报弹窗（点"生成通报"按钮）
2. 点"复制图片"按钮
3. 图片自动复制到剪贴板，可直接 Ctrl+V 粘贴到微信
4. 如果复制失败，会自动下载图片文件

#### 图片内容说明
- **标题栏**：深蓝色背景（#1e3a5f），左侧"GPT 数据总览"，右侧"全区均分 XX 分"
- **表头**：深蓝色背景 + 白色文字（中心、得分、管幅、超目标、效能异常、绩效异常、连续出勤、长期未出勤）
- **数据行**：斑马纹（白色/#f0f7ff），得分列浅色背景（绿/黄/红）
- **标红规则**：绩效异常>1个、薪资异常>3%、连续出勤>3%、长期未出勤>3% 红色加粗

---

*如有问题请联系刘洋*
