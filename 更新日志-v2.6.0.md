# GPT 每日通报可视化看板 - 更新日志

## v2.6.0 - 2026年5月21日

### 🎉 新增功能

#### 1. 班组负责人 - 批量编辑模式
- **之前**：修改一个负责人，立即保存一次（频繁调用 GitHub API）
- **现在**：修改后暂存本地，黄色提示条提醒"有未保存的修改"，等你填完所有需要修改的负责人后，再点"保存到云端"统一上传
- **好处**：减少 GitHub API 调用次数，支持一次性修改多个负责人再保存

#### 2. 离开考勤界面时 - 保存提醒
- 从"中心考勤"切换到"数据看板"或"数据明细"时，如果有未保存的负责人修改，会弹窗提醒是否保存
- 刷新页面或关闭浏览器时，也会提醒保存（防止修改丢失）

#### 3. 未保存修改 - 刷新不丢失
- 如果修改了负责人但没点"保存到云端"，刷新页面后修改仍在（黄色提示条还在），不会丢失

---

### 🔧 功能优化

#### 1. 协作数据保存 - UTF-8 编码修复
- **问题**：之前保存排休计划、未出勤原因等协作数据后，刷新页面中文内容会乱码（如"郑州"变成乱码）
- **修复**：改用正确的 UTF-8 解码方式，中文内容 now 正常显示

#### 2. 弹窗界面美化
- 原生的 `alert()` / `confirm()` 弹窗替换为 WPS 风格的美观弹窗（白色圆角 + 半透明遮罩 + 缩放动画）
- 涉及场景：切换 Tab 时提醒保存、确认操作等

#### 3. 口径说明文档更新
- 在"口径说明"面板中新增了以下章节的说明：
  - 排休计划存储方式（GitHub 仓库 `leave_plans.json`）
  - 未出勤原因存储方式（GitHub 仓库 `absence_reasons.json`）
  - 中心负责人存储方式（GitHub 仓库 `center_meta.json`）
  - 班组负责人存储方式（GitHub 仓库 `group_leaders.json`）

---

### 🐛 Bug 修复

1. **班组负责人编辑后 UI 不显示**：编辑完姓名按回车，表格中不显示新姓名（已修复，现在编辑后立即显示）
2. **刷新后未保存修改丢失**：修改负责人后没点保存，刷新页面修改消失（已修复，现在刷新后修改保留在本地）
3. **保存时二次确认弹窗**：点"保存到云端"还要再确认一次（已移除，现在直接保存）

---

### 📋 使用说明

#### 班组负责人编辑流程（新）
1. 进入"中心考勤"模块
2. 点击某个班组的负责人姓名（或"— 点击填写 —"）
3. 输入新姓名，按回车确认
4. 重复步骤 2-3，修改所有需要改的负责人
5. 点顶部黄色提示条中的"保存到云端"按钮，一次性保存所有修改

> 💡 如果不想保存，可以点黄色条中的"丢弃"按钮，恢复为云端版本

#### 协作数据说明
以下数据现在存储在 GitHub 仓库中（所有用户共享，实时同步）：
- **排休计划**：`leave_plans.json`
- **未出勤原因**：`absence_reasons.json`
- **中心负责人**：`center_meta.json`
- **班组负责人**：`group_leaders.json`

---

*如有问题请联系刘洋*
